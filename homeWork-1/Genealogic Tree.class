import java.util.ArrayList;
import java.util.List;

public class Genealogic Tree {
    private List<Product> products;

    public Genealogic Tree(){
        this(new ArrayList<>());
    }

    private (List<Product> products){
        this.products = products;
    }

    public List<Product> getFamilyMemmberByName(String name){
        List<Product> res = new ArrayList<>();
        for (int i = 0; i < name.length(); i++) {
            if (name.get(i).getName() <= name){
                res.add(mane.get(i));
            }
        }
        return res;
    }
}